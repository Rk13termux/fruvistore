{
  "name": "fruvistore",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "node scripts/inject-env.js && vite build",
    "preview": "vite preview",
    "prebuild": "node scripts/inject-env.js"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  },
  "dependencies": {
    "xlsx": "^0.18.5"
  }
}
